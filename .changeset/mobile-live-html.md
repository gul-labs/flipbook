---
'@gullabs/flipbook-core': minor
'@gullabs/react-flipbook': minor
---

Mobile live-HTML: cancel in-flight turns on resize (`cancelTurn`), stamp-then-abandon `updateSettings`, pointer-capture steal handling, and a React `destroy()` handle that retires a still-mounted book.
